The solution has been validated with Salesforce CLI v2.136.8 (win32-x64,
Node v22.19.0). The Windows x64 installer bundles Node.js, so a separate Node
installation is not required. Confirm the install on each server with sf --version.
